---
title: services
pagination:
  data: locales 
  size: 1
  alias: locale
permalink: "{% if locale.slug %}{{ locale.slug }}/{% endif %}services.html"
---

{% from "macros/serviceIcon.njk" import serviceIcon %}
{% from "macros/checkIcon.njk" import checkIcon %}

{# --- HERO --- #}
<section class="relative border-b border-slate-100 mb-14">
  <div class="absolute inset-0 bg-[url('/images/view.png')] bg-cover bg-center"></div>
  <div class="absolute inset-0 ltr:bg-gradient-to-r rtl:bg-gradient-to-l from-white/90 via-white/70 to-white/30"></div>

  <div class="relative max-w-7xl mx-auto py-20 md:py-28">
    <div class="max-w-6xl mx-auto px-4 m-6 wow fadeInUp"
     data-wow-duration=".7s">
      <p class="text-gold font-semibold">— {{ dict.slogan }}</p>
      <h1 class="text-4xl sm:text-5xl text-navy mt-2">{{ dict.services }}</h1>
      <p class="mt-4 text-slate-700 max-w-2xl">{{ dict.service.heroDesc }}</p>
    </div>

    {# CTA card: static on mobile, absolute on md+ #}
    {# <div class="m-6 md:mt-0 md:absolute md:end-6 md:top-6 md:max-w-md wow fadeInDown"
     data-wow-duration=".7s">
      <div class="rounded-xl border border-slate-200 p-6 sm:p-7 bg-white/80 backdrop-blur">
        <p class="text-sm text-slate-600">{{ dict.service.accounting.desc }}</p>
        <a href="https://wa.link/yarwxp/"
           class="mt-4 inline-block bg-gold text-navy text-base px-5 py-2.5 rounded hover:opacity-90">
          {{ dict.common['btn.requestQuote'] }}
        </a>
      </div>
    </div> #}
  </div>
</section>
{# --- CARDS GRID --- #}
<section class="max-w-6xl mx-auto px-4 py-10">
  <div class="grid md:grid-cols-2 gap-6">

    {# --- Card component --- #}
    {% macro serviceCard(id, iconName, title, bullets) %}
      <article id="{{ id }}" class="group rounded-2xl border border-slate-200 bg-white/70 backdrop-blur-sm overflow-hidden shadow-sm hover:shadow-md transition wow fadeInUp" data-wow-delay="0.5s">
        <header class="flex items-center gap-3 px-4 py-3 bg-navy/95 text-slate-100">
          <span class="inline-flex size-12 items-center justify-center rounded-xl ring-1 ring-white/10">
            {{ serviceIcon(iconName, 26) }}
          </span>
          <h2 class="text-lg sm:text-xl font-semibold">{{ title }}</h2>
        </header>

        <div class="p-6">
          {% if bullets and bullets.length %}
            <ul class="space-y-2 text-slate-700">
              {% for item in bullets %}
                <li class="flex gap-3">
                  {{ checkIcon() }}
                  <span>{{ item }}</span>
                </li>
              {% endfor %}
            </ul>
          {% endif %}

        </div>
      </article>
    {% endmacro %}

    {{ serviceCard("accounting", "accounting", dict.service.accounting.title, dict.service.accounting.bullets) }}
    {{ serviceCard("advisory", "advisory", dict.service.advisory.title, dict.service.advisory.bullets) }}
  </div>
</section>
{# 
<section class="container mx-auto py-14 border-t-2 border-slate-100 text-slate-700">
  <div class="max-w-6xl mx-auto grid md:grid-cols-2 items-center gap-10 px-4">

    <!-- Masked image (CSS mask) -->
    <div class="relative w-40 sm:w-56 md:w-64 aspect-square justify-self-center
                wow unblur-scale"
         data-wow-duration="2s"
         data-wow-offset="90">
      <div
        class="absolute inset-0 bg-[image:url('/images/view.png')] bg-cover bg-center
               [mask-image:url('/images/icon-mask.png')] [-webkit-mask-image:url('/images/icon-mask.png')]
               [mask-size:contain] [-webkit-mask-size:contain]
               [mask-repeat:no-repeat] [-webkit-mask-repeat:no-repeat]
               [mask-position:center] [-webkit-mask-position:center]
               blur-md">
      </div>
    </div>

    <!-- Text -->
    <p class="text-2xl sm:text-4xl leading-tight text-slate-800
              wow fadeInUp"
       data-wow-duration="1.5s"
       data-wow-delay="0.4s">
      {{ dict.service.advisory.desc }}
    </p>
  </div>
</section>

<section class="my-10">
  <div class="container mx-auto px-4">
    <div class="rounded-2xl border border-slate-200 bg-gradient-to-r from-slate-50 to-white shadow-sm overflow-hidden
                wow fadeInUp"
         data-wow-duration="2s"
         data-wow-offset="100">
      <div class="grid lg:grid-cols-[1fr_auto] items-center gap-6 p-6 md:p-10">

        <p class="text-xl sm:text-2xl text-slate-800 max-w-3xl
                  wow fadeIn{{ dict.css.start }}"
           data-wow-duration="1.5s"
           data-wow-delay="0.5s">
          {{ dict.service.policies.desc }}
        </p>

        <div class="flex flex-col sm:flex-row gap-3 sm:justify-end">
          <a href="https://wa.link/yarwxp/"
             class="inline-flex items-center justify-center rounded bg-gold px-5 py-3 font-medium hover:opacity-90 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-navy/60
                    wow zoomIn"
             data-wow-duration="1s"
             data-wow-delay="0.5s">
            {{ dict.common['cta.freeConsult'] }}
          </a>
        </div>
      </div>
    </div>
  </div>
</section> 
#}

